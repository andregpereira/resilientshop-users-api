spring:
  # H2
  datasource:
    url: jdbc:h2:mem:resilientshop-user-test
    username: sa
    password: sa
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
